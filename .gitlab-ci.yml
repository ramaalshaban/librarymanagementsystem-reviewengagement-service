stages:
  - deploy
  - build
  - test

build-and-deploy-server:
  stage: deploy
  image: docker:stable
  services:
    - docker:dind
  script:
    - cp $RSA_PRV_KEY  keys/rsa.key
    - sudo bash -c "docker build -f Dockerfile -t reviewEngagement-service:latest ."
    - sudo bash -c 'cd undefined/librarymanagementsystem/  && docker compose up -d reviewEngagement-service'
  only:
    - main
# Notice: This file is generated by Mindbricks and formatted by prettier-plugin-yaml
